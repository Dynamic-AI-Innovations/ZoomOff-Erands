[build]
  command = "cd ../.. && pnpm install --frozen-lockfile && pnpm --filter @zoomoff/admin build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "20"
  PNPM_VERSION = "9"

[[plugins]]
  package = "@netlify/plugin-nextjs"
